version: "3"

tasks:
  build:backend:
    desc: Build the Go backend
    dir: backend
    cmds:
      - go build ./...

  test:backend:
    desc: Run backend tests
    dir: backend
    cmds:
      - go test ./... -v

  lint:backend:
    desc: Run backend linter
    dir: backend
    cmds:
      - golangci-lint run

  bench:backend:
    desc: Run Go generator benchmarks
    dir: backend
    cmds:
      - go test -bench=. -benchtime=10x ./internal/generator/

  dev:backend:
    desc: Start backend dev server
    dir: backend
    cmds:
      - go run ./cmd/api

  build:frontend:
    desc: Build the frontend
    dir: frontend
    cmds:
      - npm run build

  test:frontend:
    desc: Run frontend tests
    dir: frontend
    cmds:
      - npm test

  dev:frontend:
    desc: Start frontend dev server (port 5180, network accessible)
    dir: frontend
    cmds:
      - npm run dev -- --host 0.0.0.0

  build:
    desc: Build backend and frontend
    deps:
      - build:backend
      - build:frontend

  test:
    desc: Run all tests
    deps:
      - test:backend
      - test:frontend

  deploy:
    desc: Deploy infrastructure via Terraform
    deps:
      - build
    dir: infra
    cmds:
      - terraform apply
